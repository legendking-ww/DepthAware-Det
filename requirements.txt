# PyTorch: install separately for your CUDA version, e.g.:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

ultralytics>=8.0.0
opencv-python>=4.8.0
matplotlib>=3.7.0
numpy>=1.24.0
onnx>=1.14.0
onnxruntime-gpu>=1.16.0
timm>=0.9.0
huggingface_hub>=0.20.0
Pillow>=10.0.0

# Web UI
gradio>=4.0.0

# Optional (TensorRT / PyCUDA — install from NVIDIA if needed)
# tensorrt==8.6.1
# pycuda
